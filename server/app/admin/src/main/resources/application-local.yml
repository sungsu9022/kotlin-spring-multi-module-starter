logging:
  level:
    root: INFO
    org:
      springframework:
        boot:
          autoconfigure: ERROR

spring:
  config:
    activate:
      on-profile: local
  devtools:
    livereload:
      enabled: true
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
